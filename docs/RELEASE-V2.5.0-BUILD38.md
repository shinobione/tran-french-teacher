# Release v2.5.0 · Build 38 — certification slice

Status: **RELEASE CANDIDATE — not accepted until merge + exact-SHA Pages success**

## Why this is a minor release

Build 38 is now a coherent, closed product milestone: deterministic Generalization & Transfer. Its internal roadmap slices are not SemVer patches. The public release therefore moves from `v2.4.0 · Build 36` to the coherent candidate `v2.5.0 · Build 38` in one explicit release-version slice.

The pedagogy baseline remains `v2.3.0 · Build 34`.

## Accepted base before this candidate

- `main`: `8790eb059f47bcde0ea399962f7b42a3b9501714`
- Build 38 milestone closeout: PR #205
- GitHub Pages: #271 / run `32068419523` — SUCCESS on that exact SHA
- Build 38.11: **NOT AUTHORIZED**

## Product represented by this release

- Curriculum: 52 lessons / 313 items
- Scenario: 44 situations / 132 turns
- Speaking Loop: 52/52, maximum 2 moments per lesson
- Listening: 0.88 normal / 0.65 slow
- Recovery: 7 durable stores / backup v3
- Evidence: derived shadow only; original six stores remain product truth
- Premium V5.10: closed / physical field pass

Build 38 certified chain:

1. 38.1 → 38.2 — subject substitution → lesson 33
2. 38.3 → 38.8 — affirmation → negation → lesson 34
3. 38.4 → 38.5 — present → futur proche → lesson 35
4. 38.6 → 38.7 — singular → plural nominal → lesson 13
5. 38.9 → 38.10 — `nous` → spoken `on` → lesson 52

## Release-only implementation scope

This slice may change only release ownership, certification, and handoff material:

- `src/core/build31-loader.js` — release cache-bust for the successor loader only; historical Build31 identity stays unchanged
- `src/core/build32-loader.js` — public `RUNTIME_META` only: `2.5.0 / 38`; historical Build32 loader identity stays unchanged
- runtime-version tribunal/workflow
- `release-v2.5.json`
- dedicated release contract test/workflow
- this document, README, PROJECT-STATE

It must not modify curriculum, Foundations, Transfer semantics, learner stores, Recovery schema, Evidence role, voices, Premium UI, `index.html`, `sw.js`, manifest, logo, or favicon.

## Historical learner continuity — release invariant

The machine-readable release contract preserves the historical continuity checkpoint:

- 7 completed lessons
- `l8` progress = 4
- 40 known historical items

A version label is never authority to reset learner data.

## Certification requirements

Before merge:

1. static release contract passes;
2. Build 38.10 predecessor contract passes;
3. public runtime metadata is exactly `v2.5.0 · Build 38`;
4. pedagogy baseline remains exactly `v2.3.0 · Build 34`;
5. Foundations refresh cannot reclaim/overwrite release metadata;
6. Settings visibly renders `v2.5.0 • Build 38`;
7. browser tribunal passes VI/FR × desktop/iPhone;
8. release scope guard proves no product-semantic files changed;
9. full repository matrix introduces no new failure beyond the documented inherited baseline.

After merge:

1. verify `main` exact merge/squash SHA;
2. require GitHub Pages SUCCESS on that exact SHA;
3. only then call `v2.5.0 · Build 38` the accepted public runtime release;
4. perform a separate docs-only closeout to reconcile MASTER-ROADMAP, PROJECT-STATE, CHANGELOG, and release-versioning policy.

## Explicit non-claims

This release slice does not add pedagogy. It does not upgrade Trân's CEFR level, modify mastery, migrate storage, alter speech recognition, change Tyffany, or touch the field-approved Premium shell. It certifies and publishes the coherent Build 38 milestone that is already implemented and closed.
