# Required on Vercel
OPENAI_API_KEY=sk-proj-REPLACE_ME
TUTOR_ACCESS_TOKEN=replace-with-a-long-random-private-token

# Optional overrides
OPENAI_REALTIME_MODEL=gpt-realtime
OPENAI_REALTIME_VOICE=cedar
OPENAI_MODEL=gpt-5

# Optional extra browser origins, comma-separated.
# GitHub Pages https://shinobione.github.io is already allowed by the backend.
ALLOWED_ORIGINS=
