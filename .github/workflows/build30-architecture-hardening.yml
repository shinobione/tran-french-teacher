name: Build 30 Architecture hardening

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  architecture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Static architecture contracts
        run: |
          node --check src/core/runtime-contracts.js
          node --check src/core/runtime-bridge.js
          node --check tests/smoke/build30-architecture-smoke.js
          node --check src/core/build-meta.js
          node --check sw.js
          node - <<'NODE'
          const fs=require('fs');
          const contracts=fs.readFileSync('src/core/runtime-contracts.js','utf8');
          const meta=fs.readFileSync('src/core/build-meta.js','utf8');
          const bridge=fs.readFileSync('src/core/runtime-bridge.js','utf8');
          const cv=contracts.match(/const VERSION = '([^']+)'/);
          const cb=contracts.match(/const BUILD = ([0-9.]+);/);
          const mm=meta.match(/const META = \{ version: '([^']+)', build: '([^']+)'/);
          if(!cv||!cb||!mm) throw new Error('release metadata parse failed');
          if(cv[1] !== '2.0.0' || Number(cb[1]) !== 30) throw new Error('Frozen Build 30 runtime contract moved');
          const parts=mm[1].split('.').map(Number);
          if(parts.length!==3 || parts[0]!==2 || Number(mm[2])<30) throw new Error('Current product release is not compatible with Build 30 baseline');
          if(!meta.includes('src/core/runtime-contracts.js?v=2.0.0-b30')) throw new Error('build-meta frozen contract import mismatch');
          if(!meta.includes('src/core/runtime-bridge.js?v=2.0.0-b30')) throw new Error('build-meta frozen bridge import mismatch');
          if(!bridge.includes('src/core/runtime-contracts.js?v=2.0.0-b30')) throw new Error('bridge frozen contract import mismatch');
          console.log(`Build 30 architecture frozen under current product ${mm[1]} / Build ${mm[2]}`);
          NODE
          grep -Fq 'src/core/runtime-contracts.js?v=${B30}' sw.js
          grep -Fq 'src/core/runtime-bridge.js?v=${B30}' sw.js
          grep -Fq 'tests/smoke/build30-architecture-smoke.js?v=${B30}' sw.js
          grep -q "learner: 'francais-avec-luc:learner:v1'" src/core/runtime-contracts.js
          grep -q 'lessons: 40' src/core/runtime-contracts.js
          grep -q 'items: 241' src/core/runtime-contracts.js
          grep -q 'scenarios: 36' src/core/runtime-contracts.js
          grep -q 'scenarioTurns: 108' src/core/runtime-contracts.js
          grep -q 'listeningNormal: 0.88' src/core/runtime-contracts.js
          grep -q 'listeningSlow: 0.65' src/core/runtime-contracts.js
          ! grep -q 'localStorage.setItem' src/core/runtime-contracts.js
          ! grep -q 'localStorage.setItem' src/core/runtime-bridge.js

      - name: Legacy core, voice and branding sanctuaries stay byte-identical
        run: |
          test "$(git hash-object app.js)" = "600f094266c9f0c4c7b57efdbf61129909ebd9cb"
          test "$(git hash-object assets/LOGO.png)" = "64eaa6ad9781c6a9075d4f68615fc44344c4e21c"
          test "$(git hash-object assets/Favicon.png)" = "c358672368a960bf7617e5532aff3e3319cddb3e"
          test "$(git hash-object voice-ios.js)" = "38e97aa3ef62dd6dcda224901b435f0973618679"
          test "$(git hash-object free-voice.js)" = "b4c19b1936c788ee017eac9ba14e5a62c159e8d5"

      - name: Start static server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 >/tmp/ft-b30-http.log 2>&1 &
          echo $! >/tmp/ft-b30-server.pid
          sleep 1
          google-chrome --version

      - name: Chrome desktop — runtime boundary owns contracts without learner writes
        run: |
          passed=0
          for attempt in 1 2 3; do
            profile="/tmp/ft-b30-desktop-$attempt"
            file="/tmp/b30-desktop-$attempt.html"
            rm -rf "$profile" "$file"
            timeout --signal=TERM --kill-after=5s 65s \
              google-chrome --headless=new --no-sandbox --disable-gpu \
              --disable-background-timer-throttling --disable-renderer-backgrounding \
              --user-data-dir="$profile" --window-size=1440,900 \
              --virtual-time-budget=36000 --dump-dom \
              'http://127.0.0.1:4173/?debug=fr&b30Audit=1' >"$file" || true
            ok=1
            for marker in \
              'data-b30-complete="1"' \
              'data-b30-runtime-ready="1"' \
              'data-b30-lessons="40"' \
              'data-b30-items="241"' \
              'data-b30-missing-required="0"' \
              'data-b30-owner-unique="1"' \
              'data-b30-store-unique="1"' \
              'data-b30-learner-key="1"' \
              'data-b30-product-contract="1"' \
              'data-b30-speaking-ready="1"' \
              'data-b30-recovery-ready="1"' \
              'data-b30-shell-ready="1"' \
              'data-b30-progress-route="1"' \
              'data-b30-home-route="1"' \
              'data-b30-practice-route="1"' \
              'data-b30-learner-unchanged="1"' \
              'data-b30-nav-single-active="1"' \
              'data-b30-horizontal-overflow="0"'; do
              grep -q "$marker" "$file" || ok=0
            done
            grep -Eq 'data-b30-version="[^\"]+-30"' "$file" || ok=0
            if grep -E -q 'id="app"[^>]*><div class="boot-status error' "$file"; then ok=0; fi
            if [ "$ok" = 1 ]; then passed=1; break; fi
            echo '=== Build 30 desktop markers ==='
            grep -o 'data-b30-[^ >]*' "$file" | head -80 || true
          done
          test "$passed" = 1

      - name: Chrome mobile 390x844 — same boundary, no overflow
        run: |
          passed=0
          for attempt in 1 2 3; do
            profile="/tmp/ft-b30-mobile-$attempt"
            file="/tmp/b30-mobile-$attempt.html"
            rm -rf "$profile" "$file"
            timeout --signal=TERM --kill-after=5s 65s \
              google-chrome --headless=new --no-sandbox --disable-gpu \
              --disable-background-timer-throttling --disable-renderer-backgrounding \
              --user-data-dir="$profile" --window-size=390,844 \
              --virtual-time-budget=36000 --dump-dom \
              'http://127.0.0.1:4173/?b30Audit=1' >"$file" || true
            ok=1
            grep -q 'data-b30-complete="1"' "$file" || ok=0
            grep -q 'data-b30-runtime-ready="1"' "$file" || ok=0
            grep -q 'data-b30-learner-unchanged="1"' "$file" || ok=0
            grep -q 'data-b30-horizontal-overflow="0"' "$file" || ok=0
            grep -q 'data-b30-nav-single-active="1"' "$file" || ok=0
            grep -Eq 'data-b30-version="[^\"]+-30"' "$file" || ok=0
            if [ "$ok" = 1 ]; then passed=1; break; fi
            echo '=== Build 30 mobile markers ==='
            grep -o 'data-b30-[^ >]*' "$file" | head -80 || true
          done
          test "$passed" = 1

      - name: Stop static server
        if: always()
        run: |
          if [ -f /tmp/ft-b30-server.pid ]; then kill "$(cat /tmp/ft-b30-server.pid)" || true; fi
